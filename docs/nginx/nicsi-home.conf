server {
    listen 80;
    server_name 10.24.252.33;
    root /var/www/nicsi;
    index index.html;

    location = / {
        return 302 /home/;
    }

    location = /home {
        return 301 /home/;
    }

    location /home/_next/static/ {
        access_log off;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable";
        try_files $uri =404;
    }

    location ~* ^/home/(images|logos|icons|pdfs|maps|photos|videos)/ {
        access_log off;
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
        try_files $uri =404;
    }

    location /home/ {
        try_files $uri $uri/ /home/index.html;
    }
}
